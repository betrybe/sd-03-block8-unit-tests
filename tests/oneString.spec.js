const assert = require('assert');
const oneString = require('../src/oneString');

describe('#oneString', () => {
  it('should return one string with its content well formatted', () => {

    // ESCREVA SEUS TESTES AQUI
    
  });
});